services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: demo-uppy-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./data/demo_uploader.db
      - SECRET_KEY=your-secret-key-change-in-production
      - UPLOAD_DIR=/app/data
    volumes:
      - ./data:/app/data
    networks:
      - demo-uppy-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: demo-uppy-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - demo-uppy-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  demo-uppy-network:
    driver: bridge
